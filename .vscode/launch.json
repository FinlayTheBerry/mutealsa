{
  "version": "2.0.0",
  "configurations": [
    {
      "name": "MuteAlsa (Build and Debug)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/mutealsa_debug",
      "args": [ "pre" ],
      "MIMode": "gdb",
      "miDebuggerArgs": "--nx",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build_task"
    }
  ]
}